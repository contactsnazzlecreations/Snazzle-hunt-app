name: Patch Snazzle stable UI v100

on:
  push:
    paths:
      - '.github/workflows/patch-quick-menu.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Patch stable Snazzle UI
        run: python .github/scripts/patch_ui_v100.py

      - name: Commit patched app
        run: |
          if git diff --quiet; then
            echo "No changes needed"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app.js
          git commit -m "Apply stable premium Home v100 and Snazzle splash"
          git push

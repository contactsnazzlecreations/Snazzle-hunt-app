name: Deploy Firebase Security

on:
  push:
    branches: [main]
    paths:
      - 'functions/**'
      - 'firestore.rules'
      - 'firebase.json'
      - '.firebaserc'
      - '.github/workflows/deploy-firebase-security.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: firebase-security-production
  cancel-in-progress: false

jobs:
  deploy-security:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Firebase CLI and Functions dependencies
        shell: bash
        run: |
          npm install --global firebase-tools
          npm install --prefix functions --omit=dev

      - name: Validate Firebase configuration
        shell: bash
        run: |
          node --check functions/bootstrap.js
          node --check functions/index.js
          node --check functions/hunt-codes.js
          node --check functions/meetups.js
          node --check functions/admin-mfa.js
          node --check functions/org-hunts.js
          test -f firestore.rules
          test -f firebase.json
          test -f .firebaserc

      - name: Authenticate and deploy security backend
        shell: bash
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          FIREBASE_SERVICE_ACCOUNT_SNAZZLE_HUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_SNAZZLE_HUNT }}
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
        run: |
          set -euo pipefail
          SA_JSON="${FIREBASE_SERVICE_ACCOUNT_SNAZZLE_HUNT:-${FIREBASE_SERVICE_ACCOUNT:-}}"

          deploy_with_auth() {
            if [ -n "$SA_JSON" ]; then
              CREDS="$RUNNER_TEMP/firebase-service-account.json"
              printf '%s' "$SA_JSON" > "$CREDS"
              chmod 600 "$CREDS"
              export GOOGLE_APPLICATION_CREDENTIALS="$CREDS"
              firebase deploy --project snazzle-hunt --only "$1" --non-interactive
            elif [ -n "${FIREBASE_TOKEN:-}" ]; then
              firebase deploy --project snazzle-hunt --only "$1" --non-interactive --token "$FIREBASE_TOKEN"
            else
              echo 'Geen Firebase deployment credential gevonden.' >&2
              echo 'Voeg een repository secret FIREBASE_SERVICE_ACCOUNT_SNAZZLE_HUNT, FIREBASE_SERVICE_ACCOUNT of FIREBASE_TOKEN toe.' >&2
              exit 1
            fi
          }

          # Kritische volgorde: publiceer eerst de Functions die MFA en privacy ondersteunen.
          # Pas wanneer dat volledig slaagt mogen de strengere Firestore-regels live gaan.
          # Zo kan een mislukte Functions-deploy nooit Beheer blokkeren.
          deploy_with_auth functions
          deploy_with_auth firestore:rules

# Security deploy trigger: 2026-08-25 Snazzle Nieuws v46
# Security redeploy trigger: 2026-08-25 public Snazzle-look v64
# Luisterverhalen permission redeploy: 2026-08-25 19:45
# Security hardening v141: MFA, meetup privacy and strict photo validation
# Safety order v141: Functions must succeed before stricter Firestore rules are published
# Organization platform v143: validate org-hunts.js before any production deployment
